 *. 先用 git add <file> 把文件加入缓冲区.
 
 *. git push 总的意图是将本地缓冲区中的东西推向网络.
 
 *. 管道. "|". 前一个命令的输出, 是后一个命令的输入.
 *. ">" 将前面显示的内容变成文件, 后面是文件名.
 
 *. 在一个仓库的目录下, 有一个 .git 的子目录. 
 *. 数据对象和树对象. 数据对象对应一个文件, 树对象对应一个目录.
 
 *. 只添加数据. 
 
 *. 三个状态. 
   #. 如何感知到修改?
   #. 暂存区是一个文件? 就当作一个窗口好了.

 *. 仓库是一个概念. 它的存储形式是一个目录. 远程的服务器只有一份儿. 但所有的工作站都是副本.
   #. $ git clone git://git.kernel.org/pub/scm/.../linux.git my-linux 克隆到本地, 换一个目录名.
   #. .git 目录在库目录下面.