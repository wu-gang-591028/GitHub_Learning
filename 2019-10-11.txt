 *. 在网站上建立一个分支. 在本机上第一次 fetch 时, 先下载分支列表. 再选择所要的分支. 这时, 要在网站上产生一个 pull request 才行.
 *. 选择一个分支下载后, 工作目录中的文件自动对齐.
 
 *. 数据对象. 对应文件.
 *. 树对象. 下面是多条树对象记录. 记录指向数据对象和子树对象, 还有它们的名字.
 *. 提交对象, 指向顶层树对象和父提交对象.
 
    这样就构成一个完整的链条.
 